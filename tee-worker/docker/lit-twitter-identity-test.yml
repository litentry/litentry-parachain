services:
    lit-twitter-identity-test:
        image: litentry/litentry-cli:latest
        container_name: litentry-twitter-identity-test
        volumes:
            - ../ts-tests:/ts-tests
            - ../client-api:/client-api
            - ../cli:/usr/local/worker-cli
        build:
            context: ..
            dockerfile: build.Dockerfile
            target: deployed-client
        environment:
          - TWITTER_OFFICIAL_URL=http://localhost:19527
          - TWITTER_AUTH_TOKEN_V2=
          - TWITTER_CLIENT_ID=client_id
          - TWITTER_CLIENT_SECRET=client_secret
        depends_on:
            litentry-node:
                condition: service_healthy
            litentry-worker-1:
                condition: service_healthy
        networks:
            - litentry-test-network
        entrypoint: "bash -c '/usr/local/worker-cli/lit_ts_integration_test.sh twitter_identity.test.ts 2>&1' "
        restart: "no"
networks:
    litentry-test-network:
        driver: bridge
