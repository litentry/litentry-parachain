{
	"name": "sidechain-api",
	"type": "module",
	"license": "ISC",
	"main": "dist/src/index.js",
	"scripts": {
		"clean": "rm -rf dist build node_modules",
		"update-metadata": "../../bin/litentry-cli print-sgx-metadata-raw > build/litentry-sidechain-metadata.json",
		"prepare-dir": "mkdir -p build && cp -rf prepare-build/* build",
		"generate-from-defs": "pnpm exec ts-node --esm node_modules/.bin/polkadot-types-from-defs  --package sidechain-api/interfaces --input build/interfaces --endpoint build/litentry-sidechain-metadata.json",
		"generate-from-chain": "pnpm exec ts-node --esm node_modules/.bin/polkadot-types-from-chain --package sidechain-api/interfaces --output build/interfaces --endpoint build/litentry-sidechain-metadata.json --strict",
		"format": "pnpm exec prettier --write .",
		"check-format": "pnpm exec prettier --check .",
		"compile": "tsc",
		"build": "pnpm run prepare-dir && pnpm run generate-from-defs && pnpm run generate-from-chain && pnpm run format && pnpm run compile"
	},
	"dependencies": {
		"@polkadot/api": "^10.9.1"
	},
	"devDependencies": {
		"@polkadot/typegen": "^10.9.1",
		"@types/node": "^20.3.1",
		"prettier": "2.8.1",
		"ts-node": "^10.9.1",
		"typescript": "^5.0.4"
	},
	"packageManager": "pnpm@8.7.6"
}
