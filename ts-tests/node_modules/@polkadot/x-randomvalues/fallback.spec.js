"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _fallback = _interopRequireDefault(require("./fallback"));

// Copyright 2017-2020 @polkadot/x-randomvalues authors & contributors
// SPDX-License-Identifier: Apache-2.0
describe('fallback (insecure)', () => {
  it('subsequent results does not match', () => {
    expect((0, _fallback.default)(new Uint8Array(32)).toString()).not.toEqual((0, _fallback.default)(new Uint8Array(32)).toString());
  });
  it('generates with the supplied length', () => {
    expect((0, _fallback.default)(new Uint8Array(128))).toHaveLength(128);
  });
});