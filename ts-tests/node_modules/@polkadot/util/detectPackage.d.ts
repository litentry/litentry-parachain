interface PackageJson {
    name: string;
    version: string;
}
/**
 * @name detectPackage
 * @summary Checks that a specific package is only imported once
 */
export declare function detectPackage({ name, version }: PackageJson, path?: string | false): void;
export {};
