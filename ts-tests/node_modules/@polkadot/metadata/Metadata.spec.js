"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _create = require("@polkadot/types/create");

var _static = _interopRequireDefault(require("./static"));

var _Metadata = require("./Metadata");

// Copyright 2017-2020 @polkadot/metadata authors & contributors
// SPDX-License-Identifier: Apache-2.0
describe('Metadata', () => {
  it('allows creation from hex with JSON equivalence', () => {
    const test = new _Metadata.Metadata(new _create.TypeRegistry(), _static.default);
    expect(new _Metadata.Metadata(new _create.TypeRegistry(), test.toHex()).toJSON()).toEqual(test.toJSON());
  });
});