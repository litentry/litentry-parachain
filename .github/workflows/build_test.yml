name: Rust

on:
  push:
    branches: [ rococo-v1 ]
  pull_request:
    branches: [ rococo-v1 ]
  repository_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build-test:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    - uses: actions/setup-node@v2.1.4
    - name: Add wasm toolchain
      uses: actions-rs/toolchain@v1
      with:
        toolchain: nightly
        target: wasm32-unknown-unknown
        override: true
    - name: Build relay node and parachain
      run: |
        cargo update -p pallet-account-linker
        cargo update -p pallet-offchain-worker
        cargo build --release
        cd polkadot
        cargo build --release --features=real-overseer
        cd ..
    - name: Run integration test
      run: |
        cd ts-tests
        npm install 
        npm test
    - name: Create issue on failure (for repository_dispatch only)   
      if: github.event.action == 'integration-test-trigger' && failure() 
      uses: JasonEtco/create-an-issue@v2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        

